{
  "name": "@context/router",
  "version": "1.0.0",
  "private": true,
  "description": "context-router: the Cloudflare Worker fronting https://context.lc (the Expo web app, plus the Convex auth routes).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "~5.9.2",
    "vitest": "^2",
    "wrangler": "^4.124.0"
  }
}
