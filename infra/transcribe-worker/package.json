{
  "name": "@context/transcribe-worker",
  "version": "1.0.0",
  "private": true,
  "description": "context-transcribe: the Cloudflare Worker that turns one chunk of meeting audio into text using a Workers AI binding. Holds no state, writes nothing, and never persists the audio.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "~5.9.2",
    "vitest": "^2",
    "wrangler": "^4.124.0"
  }
}
