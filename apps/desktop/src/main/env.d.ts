/**
 * Substituted by esbuild's `define` at bundle time — see `scripts/build.mjs`.
 * There is no runtime declaration of this identifier; referencing it outside a
 * bundle esbuild processed is a `ReferenceError`, which is the point: this
 * value never means anything read live from `process.env` in a packaged app.
 */
declare const __CONTEXT_DESKTOP_SIGNED__: boolean;
