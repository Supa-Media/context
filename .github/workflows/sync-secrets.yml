# 1Password → GitHub Environment secrets.
#
# The middle hop of the canonical Supa flow, which this repo has been missing:
#
#   1Password  ──sync (this workflow)──▶  GitHub Environment secrets
#                                                │
#                                                ├─▶ Convex   (deploy-convex.yml)
#                                                ├─▶ Workers  (deploy-mcp.yml, …)
#                                                └─▶ EAS      (deploy-mobile-update.yml)
#
# The framework's `docs/SECRETS.md` is the normative description and this is a
# thin wrapper on its reusable workflow. Until now Context had only the two
# ends: `pnpm setup:secrets` locally, and the deploy workflows pushing GitHub
# secrets onward. Nothing filled the middle, so every GitHub secret here was
# typed in by hand — 1Password was the documented source of truth with nothing
# reading from it, and no way to notice drift.
#
# ## Why this never runs on push
#
# **1Password rate-limits reads.** A sync on every push gets throttled and then
# starts failing deploys for a reason nobody will guess. So this is
# `workflow_dispatch` only, plus a `push:` trigger scoped to the allowlist
# file — a merge that adds or removes a secret NAME re-syncs, and a merge that
# touches anything else does not.
#
# ## The hazard, stated once and loudly
#
# **An `optional` key that is absent from the vault is DELETED from the GitHub
# environment.** That is deliberate upstream — it stops a deploy reading a
# stale value after somebody removes an item from 1Password — and it means this
# workflow can destroy a secret that was set by hand and never added to the
# vault.
#
# So `dry-run` defaults to **true**. The first run of any change prints what
# would happen and touches nothing; a real sync is an explicit choice. That is
# the right default for an operation whose failure mode is silent deletion of a
# credential, and it costs one extra dispatch.
#
# Before the first real run, make sure every name in
# `scripts/secrets-allowlist.json` exists in the `Context` vault — including
# any that were set directly in GitHub.
#
# ## What this needs, once
#
#   OP_SERVICE_ACCOUNT_TOKEN  a 1Password service-account token, READ-ONLY and
#                             scoped to the `Context` vault alone. The single
#                             bootstrap secret: it is what authorizes this hop,
#                             so it cannot itself come through it.
#   GH_ADMIN_TOKEN            a GitHub PAT with repo admin (`secrets:write`).
#                             The default GITHUB_TOKEN cannot manage secrets,
#                             which is why this is separate rather than an
#                             oversight.
#
# Both live as repository secrets (not environment ones), so a "both" run can
# read them from either matrix leg.
name: Sync Secrets

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Which environment to sync"
        type: choice
        options: [production, staging, both]
        default: production
      dry-run:
        description: "Preview only — no secret is written or deleted"
        type: boolean
        default: true

  # A merge that changes WHICH secrets exist re-syncs; nothing else does. Still
  # a dry run by default is not possible on a push trigger, so this one really
  # writes — which is why the allowlist is the only path that triggers it, and
  # why a change to it should be dispatched as a dry run first.
  push:
    branches: [main]
    paths:
      - "scripts/secrets-allowlist.json"
      - ".github/workflows/sync-secrets.yml"

# Never let two syncs of the same environment race — a manual rotation
# overlapping an on-merge run would interleave writes to the same secrets.
concurrency:
  group: sync-secrets-${{ github.event.inputs.environment || 'production' }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  sync:
    uses: Supa-Media/supa-framework/.github/workflows/sync-secrets.yml@main
    with:
      vault: Context
      allowlist-path: scripts/secrets-allowlist.json
      environment: ${{ github.event.inputs.environment || 'production' }}
      # A push-triggered run is a real sync (see the trigger comment); a
      # dispatch is a dry run unless somebody says otherwise.
      dry-run: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.dry-run == 'true' }}
    secrets:
      OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
      GH_ADMIN_TOKEN: ${{ secrets.GH_ADMIN_TOKEN }}
