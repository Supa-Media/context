{
  "name": "@context/desktop-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "window.desktop, typed and versioned: the surface the one UI uses to reach the native capabilities of the desktop shell that hosts it",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/contract.ts",
    "./fake": "./src/fake.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types test/test.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint configured yet\" && exit 0"
  },
  "dependencies": {
    "@context/meetings": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=22.6"
  }
}
